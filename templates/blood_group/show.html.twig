{% extends 'base.html.twig' %}

{% block title %}BloodGroup{% endblock %}

{% block body %}
    <h1>BloodGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blood_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ blood_group.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ blood_group.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_blood_group_index') }}">back to list</a>

    <a href="{{ path('app_blood_group_edit', {'id': blood_group.id}) }}">edit</a>

    {{ include('blood_group/_delete_form.html.twig') }}
{% endblock %}
