{% extends 'base.html.twig' %}

{% block title %}Requester{% endblock %}

{% block body %}
    <h1>Requester</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ requester.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ requester.description }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ requester.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ requester.lastName }}</td>
            </tr>
            <tr>
                <th>Blood group</th>
                <td>{{ bloodGroup.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_requester_index') }}">back to list</a>

    <a href="{{ path('app_requester_edit', {'id': requester.id}) }}">edit</a>

    {{ include('requester/_delete_form.html.twig') }}
{% endblock %}
