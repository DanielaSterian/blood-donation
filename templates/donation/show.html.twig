{% extends 'base.html.twig' %}

{% block title %}Donation{% endblock %}

{% block body %}
    <h1>Donatie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ donation.id }}</td>
            </tr>
            <tr>
                <th>Mesaj</th>
                <td>{{ donation.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_donation_index') }}">Inapoi la lista</a>

    <a href="{{ path('app_donation_edit', {'id': donation.id}) }}">Editeaza</a>

    {{ include('donation/_delete_form.html.twig') }}
{% endblock %}
